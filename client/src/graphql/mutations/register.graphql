mutation Register(
  $username: String!
  $password: String!
  $displayName: String!
) {
  register(
    input: {
      username: $username
      password: $password
      displayName: $displayName
    }
  ) {
    errors {
      field
      message
    }
    user {
      ...RegularUser
    }
  }
}
