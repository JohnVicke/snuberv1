import React from 'react';
import { MainNavigator } from './MainNavigator';

export const AuthenticationSwitch: React.FC = ({}) => {
  // Token stuff should be run here
  return <MainNavigator />;
};
